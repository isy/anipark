version: "3"

volumes:
  vendor:

services:
  be:
    build:
      context: .
      dockerfile: docker/api/Dockerfile
    volumes:
      - ./src/be:/go/src/github.com/isy/ownedia/
      - vendor:/go/src/github.com/isy/ownedia/vendor
    ports:
      - 3001:3001
    tty: true
